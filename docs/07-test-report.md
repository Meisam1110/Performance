# ۷. گزارش آزمون — تطبیق خروجی سیستم با Merit.xlsb

```
npm test        # موتور محاسبه در برابر مقادیر واقعی فایل
npm run test:e2e   # اپلیکیشن ساخته‌شده در مرورگر واقعی
```

هر دو مجموعه در آخرین اجرا **کاملاً موفق** بودند.

---

## ۷.۱ روش

مقادیر مرجع مستقیماً از `Merit.xlsb` استخراج شده‌اند
(`tools/extract_merit.py` → `tests/merit-expected.json`) — یعنی آزمون در برابر
اعدادی اجرا می‌شود که Excel واقعاً تولید کرده، نه اعدادی که ما انتظار داشتیم.

آزمون در دو سناریو اجرا می‌شود:

* **سناریو A — بازپخش تطابقی.** ورودی شماره پرسنلی ۴ همان چیزی است که فرمول ستون O
  عملاً روی آن عمل کرده (بدون اثرگذاری ویژه — نگاه کنید به AMB-01). در این سناریو
  **هر ستون محاسباتی هر ۱۰۰ نفر** باید با Excel مطابقت کند.
* **سناریو B — قاعدهٔ مستند.** داده عیناً همان‌طور که در فایل است، با قاعده‌ای که
  سند مرجع بیان می‌کند. این همان چیزی است که سیستم تحویلی اجرا می‌کند. آزمون
  اندازهٔ دقیق انحراف AMB-01 را گزارش می‌کند.

---

## ۷.۲ سناریو A — اعداد کنترلی شیت‌ها

| شاخص | موتور | Excel | اختلاف | نتیجه |
|---|---:|---:|---:|---|
| تعداد افراد (`A5`) | ۱۰۰ | ۱۰۰ | ۰ | ✅ |
| مخزن پایه (`B5`) | ۱۰٬۰۰۰٫۰۰۰۰ | ۱۰٬۰۰۰٫۰۰۰۰ | ۰ | ✅ |
| مازاد ضریب (`O5`) | ۲۱۵٫۰۰۰۰ | ۲۱۵٫۰۰۰۰ | ۰ | ✅ |
| متناسب‌سازی هر نفر (`N5`) | ۲٫۱۵۰۰ | ۲٫۱۵۰۰ | ۰ | ✅ |
| سرشکن زیر حد نصاب (`J5`) | ۲٫۷۲۸۹۴۷۳۶۸ | ۲٫۷۲۸۹۴۷۳۶۸ | ۱٫۹×۱۰⁻¹³ | ✅ |
| سرشکن تغییرات معاون (`O5`) | −۲۶۹٬۲۳۰٬۳۳۳٫۸۹۹۳ | −۲۶۹٬۲۳۰٬۳۳۳٫۸۹۹۳ | ۰ | ✅ |
| مجموع دریافتی اولیه (`M5`) | ۹۹٬۹۹۹٬۹۹۹٬۹۹۹٫۹۹۹۹ | ۹۹٬۹۹۹٬۹۹۹٬۹۹۹٫۹۹۹۸ | ۶٫۱×۱۰⁻⁵ | ✅ |
| مجموع کارانه نهایی (`Q5`) | ۹۹٬۹۹۹٬۹۹۹٬۹۹۹٫۹۹۹۸ | ۱۰۰٬۰۰۰٬۰۰۰٬۰۰۰ | ۲٫۱×۱۰⁻⁴ | ✅ |
| تراز با بودجه | ۹۹٬۹۹۹٬۹۹۹٬۹۹۹٫۹۹۹۸ | ۱۰۰٬۰۰۰٬۰۰۰٬۰۰۰ | ۱٫۸×۱۰⁻⁴ | ✅ |

اختلاف‌های باقیمانده در حد نویز ممیز شناور IEEE-754 هستند
(روی بودجهٔ ۱۰¹¹ ریال، نسبت اختلاف حدود ۱۰⁻¹⁵ است — کمتر از یک ده‌هزارم ریال).

## ۷.۳ سناریو A — بیشینهٔ اختلاف هر ستون روی هر ۱۰۰ نفر

| ستون | بیشینهٔ اختلاف | آستانه | نتیجه |
|---|---:|---:|---|
| K — امتیاز عملکرد (شیت ۱) | ۰ | ۱۰⁻⁶ | ✅ |
| L — عدد کارانه | ۰ | ۱۰⁻⁶ | ✅ |
| N — اثرگذاری ویژه | ۰ | ۱۰⁻⁶ | ✅ ¹ |
| O — ضریب کارانه | ۰ | ۱۰⁻⁶ | ✅ |
| R — ضریب نهایی | ۲٫۸×۱۰⁻¹⁴ | ۱۰⁻⁶ | ✅ |
| F — عدد گرید (شیت ۲) | ۰ | ۱۰⁻⁶ | ✅ |
| G — تأثیر گرید | ۰ | ۱۰⁻⁶ | ✅ |
| I — عدد ارزیابی | ۰ | ۱۰⁻⁶ | ✅ |
| K — امتیاز عملکردی | ۲٫۳×۱۰⁻¹³ | ۱۰⁻⁶ | ✅ |
| L — امتیاز کل | ۲٫۳×۱۰⁻¹³ | ۱۰⁻⁶ | ✅ |
| M — دریافتی اولیه | ۱٫۹×۱۰⁻⁶ ریال | ۱۰⁻³ | ✅ |
| Q — دریافتی نهایی | ۲٫۰×۱۰⁻⁶ ریال | ۱۰⁻³ | ✅ |

¹ شماره پرسنلی ۴ از این مقایسه کنار گذاشته شده است: خود فایل در آن سلول با خودش
ناسازگار است (`N = 300` ولی ستون O طوری رفتار کرده که انگار `N = 0`). مقایسهٔ آن
یعنی ادعای هر دو نیمهٔ یک تناقض. جزئیات در AMB-01.

---

## ۷.۴ نمونهٔ ۱۰ نفره — کارانهٔ نهایی

| شماره پرسنلی | Excel (ریال) | سیستم (ریال) | اختلاف | نتیجه |
|---|---:|---:|---:|---|
| ۱ | ۱۰٬۰۰۰٬۰۰۰٬۰۰۰٫۰۰ | ۱۰٬۰۰۰٬۰۰۰٬۰۰۰٫۰۰ | ۰ | ✅ PASS |
| ۲ | ۰٫۰۰ | ۰٫۰۰ | ۰ | ✅ PASS |
| ۳ | ۳٬۹۳۶٬۵۵۹٬۱۳۹٫۷۸ | ۳٬۹۳۶٬۵۵۹٬۱۳۹٫۷۸ | ۱٫۴×۱۰⁻⁶ | ✅ PASS |
| ۴ | ۲۰٬۰۰۰٬۰۰۰٬۰۰۰٫۰۰ | ۲۰٬۰۰۰٬۰۰۰٬۰۰۰٫۰۰ | ۰ | ✅ PASS |
| ۵ | ۸۶۱٬۵۵۹٬۱۳۹٫۷۸ | ۸۶۱٬۵۵۹٬۱۳۹٫۷۸ | ۲٫۰×۱۰⁻⁶ | ✅ PASS |
| ۶ | ۴۱۱٬۵۵۹٬۱۳۹٫۷۸ | ۴۱۱٬۵۵۹٬۱۳۹٫۷۸ | ۱٫۹×۱۰⁻⁶ | ✅ PASS |
| ۷ | ۵۶۱٬۵۵۹٬۱۳۹٫۷۸ | ۵۶۱٬۵۵۹٬۱۳۹٫۷۸ | ۲٫۰×۱۰⁻⁶ | ✅ PASS |
| ۸ | ۷۸۶٬۵۵۹٬۱۳۹٫۷۸ | ۷۸۶٬۵۵۹٬۱۳۹٫۷۸ | ۲٫۰×۱۰⁻⁶ | ✅ PASS |
| ۹ | ۷۱۱٬۵۵۹٬۱۳۹٫۷۸ | ۷۱۱٬۵۵۹٬۱۳۹٫۷۸ | ۲٫۰×۱۰⁻⁶ | ✅ PASS |
| ۱۰ | ۵۶۱٬۵۵۹٬۱۳۹٫۷۸ | ۵۶۱٬۵۵۹٬۱۳۹٫۷۸ | ۲٫۰×۱۰⁻⁶ | ✅ PASS |

شماره ۲ صفر است چون امتیازش دقیقاً ۲٫۰۰ و برابر حد نصاب است.
شماره‌های ۱ و ۴ مبلغ دستی معاون بخش دارند.

---

## ۷.۵ سناریو B — اثر AMB-01

| ستون | جابه‌جایی نسبت به فایل |
|---|---:|
| امتیاز عملکرد، عدد کارانه، گرید، ارزیابی | ۰ |
| ضریب کارانه (O) | ۳۰۰ |
| ضریب نهایی (R) | ۲۹۷ |
| امتیاز عملکردی و امتیاز کل | ۲۹۶٫۸ |
| دریافتی اولیه (M) | ۲٫۹۷ میلیارد ریال |
| **کارانه نهایی (Q)** | **۱٫۹×۱۰⁻⁶ ریال** |

نتیجهٔ مهم: AMB-01 ستون‌های میانی را جابه‌جا می‌کند اما **مبلغ پرداختی هیچ‌کس تغییر
نمی‌کند**، چون فرد متأثر مبلغ دستی معاون بخش دارد و جابه‌جایی متناسب‌سازی برای همه
یکسان است و در سرشکن خنثی می‌شود.

---

## ۷.۶ ناورداهای موتور

| ناوردا | نتیجه |
|---|---|
| `SUM(کارانه نهایی) = بودجه` | ✅ |
| `SUM(امتیاز کل) = تعداد × ۱۰۰` | ✅ (۱۰٬۰۰۰٫۰۰۰۰) |
| فرد با امتیاز دقیقاً برابر حد نصاب پرداخت نمی‌گیرد | ✅ |
| هیچ دریافتی منفی در دادهٔ مرجع | ✅ |
| وضعیت بودجه = متوازن | ✅ |
| تغییر بودجه، مجدداً تراز می‌شود | ✅ |
| فعال‌کردن ضریب گرید توزیع را تغییر می‌دهد | ✅ |
| اجرای گریدمحور همچنان با بودجه تراز است | ✅ |

---

## ۷.۷ آزمون سرتاسری در مرورگر واقعی

`tests/ui.e2e.js` فایل تک‌فایلی ساخته‌شده را در Chromium باز می‌کند و ۳۶ بررسی
انجام می‌دهد. خلاصه:

| بخش | بررسی‌ها | نتیجه |
|---|---|---|
| راه‌اندازی | رندر پوسته، ۱۰ آیتم منو، بدون خطای صفحه | ✅ |
| بارگذاری داده | ۱۰۰ رکورد، ۹۵ واجد شرایط، ۵ زیر حد نصاب، ۲ تغییر معاون | ✅ |
| داشبورد | ۸ کاشی KPI، نوار بودجه، خلاصهٔ ۸ واحد سازمانی | ✅ |
| جدول پرداخت | تطابق تخصیص اولیه و کارانهٔ نهایی هر ۱۰۰ نفر با Excel | ✅ |
| پنل جزئیات | ۱۴ گام محاسبه، نمایش فرمول و ستون مرجع | ✅ |
| بازمحاسبهٔ لحظه‌ای | تغییر حد نصاب: ۹۵ → ۳۹ نفر، بودجه همچنان تراز | ✅ |
| گارد بودجه | مبلغ ۹۵ میلیاردی: ۸۴ نفر منفی، وضعیت `INVALID`، رد شدن اعتبارسنجی | ✅ |
| اجباری بودن توضیح | ثبت بدون توضیح مسدود، پیام نمایش داده شد | ✅ |
| مرکز اعتبارسنجی | ۵ نفر زیر حد نصاب گزارش شدند | ✅ |
| خروجی Excel | ۷ شیت، ترتیب ستون‌های مرجع، RTL، فریز، فیلتر، جداکنندهٔ هزارگان | ✅ |
| ماندگاری | داده و تغییرات پس از reload حفظ شدند | ✅ |

---

## ۷.۸ اعتبارسنجی مستقل فایل خروجی

فایل تولیدشده با کتابخانهٔ مستقل `openpyxl` (پایتون) باز و بررسی شد:

```
sheets: ['Employee Master', 'Consolidated Questionnaire', 'روش پرداخت کارانه',
         'Validation Report', 'Summary', 'Configuration', 'Audit Log']
روش پرداخت کارانه:  103 × 19
  freeze_panes  = C2          (سطر عنوان و دو ستون اول ثابت)
  rightToLeft   = True
  autofilter    = A1:S103
  عرض ستون A    = 13.8   |  M = 24.8
  سلول A1       = «شماره پرسنلی»  bold=True  fill=FFEEF2F7
  M2 (ریال)     = 4,024,210,526      format='#,##0'
  L2 (امتیاز)   = 402.42             format='0.00'
  مجموع پرداخت ردیف ۲ تا ۱۰۱ = 100,000,000,000.00
```

هر هفت شیت راست‌به‌چپ‌اند و پنج شیت داده‌ای فریز عنوان دارند.

> **یادداشت فنی:** نسخهٔ رایگان SheetJS نه فریز پنل می‌نویسد و نه قالب عددی سلول.
> برای همین خروجی بدون فشرده‌سازی نوشته و سپس توسط `src/xlsx-postprocess.js`
> اصلاح می‌شود (تزریق `<pane>` و ساخت `styles.xml`). این ماژول در صورت مواجهه با
> هر ساختار ناشناخته، فایل اصلی را دست‌نخورده برمی‌گرداند تا خروجی هرگز خراب نشود.
> هزینهٔ این کار بزرگ‌تر شدن فایل است (۲۶۰ کیلوبایت به‌جای ۱۰۶ کیلوبایت برای ۱۰۰ نفر).

---

## ۷.۹ آنچه آزمون نشده است

* مقایسهٔ عملی دو نسخهٔ فایل Q1 (تنها یک نسخه در دسترس بود) — AMB-10.
* ورود همزمان چند فایل پرسشنامهٔ واقعی از تیم‌های مختلف؛ منطق ادغام با دادهٔ
  ساختگی آزمایش شده اما با فایل‌های واقعی چند تیمی خیر.
* کارایی رابط کاربری با ۳٬۵۷۱ رکورد در جدول (موتور محاسبه با این حجم آزمایش شده
  است؛ جدول صفحه‌بندی ۲۵۰ ردیفی دارد).
* مرورگرهای غیر Chromium.
